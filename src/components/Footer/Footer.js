import 'react' from React

export default class Footer extends React.Component {
    render() {
        return(
            <div>
                <h1>This is the FOOTER of your React SPA.</h1>
                <p>Footer elements for your SPA will go here.</p>
            </div>
        )
    }
}