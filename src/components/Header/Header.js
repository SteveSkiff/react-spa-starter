import 'react' from React

export default class Header extends React.Component {
    render() {
        return(
            <div>
                <h1>This is the HEADER of your React SPA.</h1>
                <p>Navigation for your SPA will go here.</p>
            </div>
        )
    }
}